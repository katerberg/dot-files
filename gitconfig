[alias]
s = status
t = status
st = status
ci = commit
co = checkout
con = "!n() { git checkout -b katerberg/chore/$1;}; n"
cho = checkout
cho = checkout
cp = cherry-pick
mnf = merge --no-ff
br = branch
pu = push -u origin HEAD
nuke = reset --hard origin/HEAD
please = push --force-with-lease
branch-clean = !git fetch && git remote prune origin && git branch --merged staging | grep -v staging | xargs git branch -d
[user]
	name = Mark Katerberg
	email = katerberg@fastmail.fm
[push]
	default = simple
[pull]
	rebase = true
[rebase]
	autoStash = true
[core]
	autocrlf = false
	whitespace = cr-at-eol
